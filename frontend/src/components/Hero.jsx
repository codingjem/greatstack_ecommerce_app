import React from "react";

const Hero = () => {
    return (
        <div className="flex flex-column sm:flex-row border border-gray-400">
            {/* Hero Left Side */}
        </div>
    );
};

export default Hero;
